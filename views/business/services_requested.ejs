<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Requested Services</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> 
    <link rel="stylesheet" href="../../css/style.css">
    <!-- For the css, you need to go two directories up, hence the double "../" -->
</head>

<body>
    <%- include("../partials/business_navbar") %>
    <%- include('../partials/flash_messages') %>
    <div class="container">
        <h1>Manage Requested Services</h1>
        <p>Here are the most recently client-requested services:</p>

        <div id="requested-service-list">
            <% purchases.forEach(purchase => { %>
                <div class="service">
                    <div class="service-name"> <%= purchase.service.name %> (<%= purchase.service.price %> CAD)</div>
                    <div class="service-description"> 
                        <strong>Requested:</strong> <%= purchase.datePurchased.toLocaleString('en-US', { 
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric', 
                            hour: '2-digit', 
                            minute: '2-digit' 
                        }) %> <br> 
                        <strong>Requester:</strong> <%= purchase.user.username %> <br>
                        <strong>Status:</strong> 
                        <span style="
                        color: <%= purchase.status === 'confirmed' ? 'green' : 
                                    purchase.status === 'pending' ? 'grey' : 
                                    'red' %>;"><%= purchase.status %></span>
                    </div>
                    <% if (purchase.status === 'pending') { %>
                    <div class="options">
                        <!-- Form to update status to "rejected" -->
                        <form action="/business/services_requested/<%= purchase._id %>?_method=PATCH" method="POST">
                            <input type="hidden" name="status" value="rejected">
                            <button type="submit" class="option-box" style="background-color:crimson">Remove</button>
                        </form>
        
                        <!-- Form to update status to "confirmed" -->
                        <form action="/business/services_requested/<%= purchase._id %>?_method=PATCH" method="POST">
                            <input type="hidden" name="status" value="confirmed">
                            <button type="submit" class="option-box" style="background-color:seagreen">Confirm</button>
                        </form>
                    </div>
                    <% } %>
                </div>
            <% }) %>
        </div>
    </div>

    <div class="options" style="margin: 20px auto; text-align: center;">
        <button class="go-back-button" onclick="location.href='/business/index'">Go Back</button>
    </div>    
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" 
    integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" 
    crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" 
    integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" 
    crossorigin="anonymous"></script>
</body>
<footer>
    <%- include("../partials/footer") %>
</footer>
</html>
