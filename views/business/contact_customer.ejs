<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Customer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="../../css/style.css">
</head>
<body>
    <nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/business/index">
            <% if (businessInfo.logo) { %>
              <img src="<%= businessInfo.logo %>" alt="Business Logo" 
                   style="height: 40px; width: 40px; margin-right: 10px; border-radius: 50%; object-fit: cover;">
            <% } %>
            <%= businessInfo.name || "Default Business Name" %>
          </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" 
          aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup" style="display:flex; margin-left: auto">
            <div class="navbar-nav">
              <a class="nav-link active" href="/business/index">Home</a>
              <a class="nav-link" href="/business/services_requested">See Requested Services</a>
              <a class="nav-link" href="/business/services">Modify Services</a>
              <a class="nav-link" href="/business/edit">Edit Business Information</a> 
            </div>  
            <form action="/logout" method="POST" style="display:flex; margin-left: auto">
              <button class="btn btn-danger" style="display:flex; margin-left: auto" type="submit">Logout</button>
            </form>
          </div>
        </div>
      </nav>
      

    <div class="container my-5">
        <h1 class="text-center mb-4">Contact Customer</h1>
        <div class="card shadow-lg mb-4">
            <div class="card-body">
                <h5 class="card-title"><%= client.full_name %></h5>
                <p class="card-text">
                    <strong>Username:</strong> <%= client.username %> <br>
                    <strong>Email:</strong> <a href="mailto:<%= client.email %>"><%= client.email %></a> <br>
                    <strong>Phone:</strong> <a href="tel:<%= client.phone_number %>"><%= client.phone_number %></a>
                </p>
            </div>
        </div>

        <h3>Recent Purchases</h3>
        <div id="requested-service-list">
            <% purchases.forEach(purchase => { %>
                <div class="service">
                    <div class="service-name"> <%= purchase.service.name %> (<%= purchase.service.price %> CAD)</div>
                    <div class="service-description"> 
                        <strong>Purchased:</strong> <%= purchase.datePurchased.toLocaleString('en-US', { 
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric', 
                            hour: '2-digit', 
                            minute: '2-digit' 
                        }) %> <br> 
                        <strong>Requester:</strong> <%= purchase.user.username %> <br>
                        <strong>Status:</strong> 
                        <span style="
                        color: <%= purchase.status === 'confirmed' ? 'green' : 
                                    purchase.status === 'pending' ? 'grey' : 
                                    'red' %>;"><%= purchase.status %></span>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" 
    integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" 
    integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
<footer>
    <footer class="sticky-bottom text-center p-3 bg-dark text-white">
        <div class="text-center p-3 bg-dark text-white">
            <h5 class="text-uppercase"><%= businessInfo.name %></h5>
            <p>
                <strong>Address:</strong> <%= businessInfo.address %>, <%= businessInfo.postal_code %> <br>
                <strong>Email:</strong> <a href="mailto:<%= businessInfo.email %>"><%= businessInfo.email %></a> <br>
                <strong>Phone:</strong> <a href="tel:<%= businessInfo.phone %>"><%= businessInfo.phone %></a>
            </p>
            &copy; <%= new Date().getFullYear() %> <%= businessInfo.name %>. All Rights Reserved.
        </div>
    </footer>
    
</footer>
</html>
