<!-- 
This is Paul's responsibility.
Feel free to look at Greg's pages (Login/Register/Business_Index)
for reference; it's best to have our styling look the same.
 -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> 
    <link rel="stylesheet" href="../../css/standard.css">
    <title>Service View</title>
</head>

<body>
    <%- include("../partials/client_navbar") %>
    <div class="container">
    <h1>Service View</h1>
    <p>All purchased services</p>

    <!-- Show services from user (maybe with date of purchase) -->
    <div class="service">
        <h2>Past</h2>
        <p class="service-name">
          <% if (currentUser.service_list.length === 0) { %>
            Empty
          <% } else { %>
            <%= currentUser.service_list.join(', ') %>
          <% } %>
        </p>
    </div>

    <div class="service">
        <h2>Present</h2>
        <p class="service-name">
          <% if (currentUser.service_list.length === 0) { %>
            Empty
          <% } else { %>
            <%= currentUser.service_list.join(', ') %>
          <% } %>
        </p>
    </div>

    <div class="service">
        <h2>Future</h2>
        <p class="service-name">
          <% if (currentUser.service_list.length === 0) { %>
            Empty
          <% } else { %>
            <%= currentUser.service_list.join(', ') %>
          <% } %>
        </p>
    </div>


    <b>For more detail on the services</b>
    <div class="option-box"><a href="/client/receipts_view">Click here</a></div>
</div>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" 
integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" 
crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" 
integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" 
crossorigin="anonymous"></script>
</body>
</html>