<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<<<<<<< HEAD
    <title>Payment Info</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous"> 
    <link rel="stylesheet" href="../../css/payment.css">
</head>
<body>

    <nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="./client_index.html">Business Name</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" 
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-link active" href="./client_index.html">Home</a>
                    <a class="nav-link" href="./communication.html">Contact Us</a>
                    <a class="nav-link" href="./services_request.html">Request a Service</a>
                    <a class="nav-link" href="./services_view.html">View Your Purchase History</a>
                    <a class="nav-link" href="./receipts_view.html">View Receipts</a>
                </div>
            </div>
            <button class="btn btn-danger" style="display:flex; margin-left: auto" onclick="location.href='../login.html'">Logout</button>
        </div>
    </nav>

    <div class="container" id="cardContainer" style="display:none;">
        <!-- Cards will be dynamically added here -->
    </div>
    

    <div class="options" style="margin-top: 30px;">
        <button class="option-box" onclick="addCard()">Add New Card</button>
=======
    <title>Payment info</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> 
    <link rel="stylesheet" href="../../css/payment.css">
</head>
<body>
    
    <nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="./client_index.html">Business Name</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" 
          aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link active" href="/client/client_index">Home</a>
                <a class="nav-link" href="/client/communication">Contact Us</a>
                <a class="nav-link" href="/client/services_request">Request a Service</a>
                <a class="nav-link" href="/client/services_view">View Your Purchase History</a>
                <a class="nav-link" href="/client/receipts_view">View Receipts</a>
            </div>
          </div>
          <button class="btn btn-danger" style="display:flex; margin-left: auto" onclick="location.href='../login.html'">Logout</button>
        </div>
      </nav>
    <h1>Your Cards</h1>
    <div >
        <div class="container" >
            <div >
                <img src="../../css/Images/visa.png"  class="small-image">
                <h3>**** **** **** 1234</h3>
                <h4 class="exp">Expiry date: 01/01</h4>
                <h4 class="exp">Name: John</h4>   
            </div>
                <div >
                    <button class="option-box" onclick="removeService('service-1')">Delete</button>
                </div>
            
            

        </div>
        <div class="container">
            <div>
                <img src="../../css/Images/mastercard.png"  class="small-image" >
                <h3>**** **** **** 1234</h3>
                <h4 class="exp">Expiry date: 01/01</h4>
                <h4 class="exp">Name: John</h4>
            </div>
            <div >
                <button class="option-box" onclick="removeService('service-2')">Delete</button>
            </div>
        </div>

    </div>
    <div class="options" style="margin-top: 30px;">
        <button class="option-box" onclick="addService()">Add New Card</button>
>>>>>>> service_bill
    </div>

    <div class="options" style="margin: 20px auto; text-align: center;">
        <button class="go-back-button" onclick="goBack()">Go Back</button>
<<<<<<< HEAD
    </div>

    <script>

    // Function to add a card
    async function addCard() {
    const cardType = prompt("Enter card type: Visa or Mastercard");
    if (cardType && (cardType.toLowerCase() === "visa" || cardType.toLowerCase() === "mastercard")) {
        const cardNumber = prompt("Enter card number (**** **** **** XXXX)");
        const expiryDate = prompt("Enter expiry date (MM/YY)");
        const cardName = prompt("Enter card name:");

        // Normalize cardType to proper case (Visa or Mastercard)
        const normalizedCardType = cardType.charAt(0).toUpperCase() + cardType.slice(1).toLowerCase();

        const response = await fetch('/addCard', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ cardType: normalizedCardType, cardNumber, expiryDate, cardName })
        });

        if (response.ok) {
            alert('Card added successfully!');
            loadCards(); // Reload the cards after adding one
        } else {
            alert("Failed to add the card.");
        }
    } else {
        alert("Please enter a valid card type.");
    }
}




        // Function to load all the cards from the database
        async function loadCards() {
    try {
        const response = await fetch('/getCards');
        if (response.ok) {
            const cards = await response.json();
           

            // Call displayCards to update the DOM
            displayCards(cards);
        } else {
            console.error('Failed to fetch cards:', response.statusText);
        }
    } catch (error) {
        console.error('Error fetching cards:', error);
    }
}

function displayCards(cards) {
    const cardContainer = document.getElementById('cardContainer');
    cardContainer.style.display = 'block'; // Ensure the container is visible
    cardContainer.innerHTML = ''; // Clear existing content

    cards.forEach(card => {
        const cardElement = document.createElement('div');
        cardElement.classList.add('card');
        cardElement.innerHTML = `
            <div class="card-body">
                <img src="../../css/Images/${card.cardType}.png" class="small-image">
                <h3>**** **** **** ${card.cardNumber.slice(-4)}</h3>
                <h4>Expiry date: ${card.expiryDate}</h4>
                 <h4>${card.cardName}</h4>
                <button class="option-box" onclick="deleteCard('${card._id}')">Delete</button>
            </div>
        `;
        cardContainer.appendChild(cardElement);
    });
}
        // Function to delete a card
        async function deleteCard(cardId) {
            const response = await fetch(`/deleteCard/${cardId}`, {
                method: 'DELETE',
            });

            if (response.ok) {
                alert('Card deleted successfully!');
                loadCards(); // Reload the cards after deleting one
            } else {
                alert("Failed to delete the card.");
            }
        }

        // Load the cards when the page is loaded
        window.onload = loadCards;

        // Function to go back to the previous page
    function goBack() {
        window.history.back();
    }
    
    </script>

    


    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" crossorigin="anonymous"></script>
=======
    </div>    

    <script>
        function removeRequestedService(serviceId) {
            if (confirm("Are you sure you want to remove this service?")) {
                const serviceElement = document.getElementById(serviceId);
                serviceElement.parentNode.removeChild(serviceElement); // Remove the service from the DOM
                alert("Service has been removed from your requests.");
            }
        }
        function addCard() {
            // Prompt the user for the new service name
            const newCardName = prompt("Enter the name of the new service:");
            if (newCardName) {
                // Create a new service element
                const newCardId = 'service-' + (document.querySelectorAll('.service').length + 1); // Unique ID
                const newCardHtml = `
                    <div class="service" id="${newServiceId}">
                        <div class="service-name">Service:</div>
                        <p class="service-description">${newServiceName}</p>
                        <div class="options">
                            <button class="option-box" onclick="modifyService('${newServiceId}')">Modify</button>
                            <button class="option-box" onclick="removeService('${newServiceId}')">Delete</button>
                        </div>
                    </div>
                `;

                // Append the new service element to the service list
                document.getElementById('service-list').insertAdjacentHTML('beforeend', newServiceHtml);
                alert("New service has been added: " + newServiceName);
            }
        }

        // Function to go back to the previous page
        function goBack() {
            window.history.back();
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" 
    integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" 
    crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" 
    integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" 
    crossorigin="anonymous"></script>
>>>>>>> service_bill
</body>
</html>