<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Receipts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" 
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> 
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <%- include('../partials/client_navbar') %>

    <div class="container my-5">
        <h1 class="text-center mb-4">My Receipts</h1>

        <% if (confirmedPurchases.length > 0) { %>
            <div class="service-list">
                <% confirmedPurchases.forEach(purchase => { %>
                    <div class="col-md-6 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">Receipt</h5>
                                <p class="card-text"><strong>Service:</strong> <%= purchase.service.name %></p>
                                <p class="card-text"><strong>Price:</strong> $<%= purchase.service.price.toFixed(2) %></p>
                                <p class="card-text"><strong>Tax (15%):</strong> $<%= (purchase.service.price * 0.15).toFixed(2) %></p>
                                <p class="card-text"><strong>Total:</strong> $<%= (purchase.service.price * 1.15).toFixed(2) %></p>
                                <p class="card-text"><strong>Date Purchased:</strong> <%= new Date(purchase.datePurchased).toLocaleDateString() %></p>
                                <p class="card-text"><strong>Status:</strong> <%= purchase.status %></p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>No confirmed purchases at the moment.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" 
        integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" 
        integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
<footer>
    <%- include("../partials/footer") %>
  </footer>
</html>
